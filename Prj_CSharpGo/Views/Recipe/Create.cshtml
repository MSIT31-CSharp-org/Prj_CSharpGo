@using Prj_CSharpGo.Models.ViewModels

@model RecipeProduct

@{
    ViewData["Title"] = "Create";
}

<div class="container py-5">
    <div class="row py-5">
        <form class="col-md-9 m-auto" method="post" action="/Recipe/Create" data-ajax='false'>
            @*<form class="col-md-9 m-auto" method="post" action="/Recipe/Edit" role="form" data-ajax='false'>*@
            @*上傳圖片 JS在最下面*@
            <img id="output" style="width: 400px; height: 300px; margin-left:50px" class="form-control mt-1" />
            <input type="file" name="Img" accept="image/*" onchange="loadFile(event)">
            <br> <br>
            @*上傳圖片*@
            <div class="row">
                <div class="form-group col-md-6 mb-3">
                    <label for="inputname">RecipeName</label>
                    <input type="text" class="form-control mt-1" name="RecipeName" placeholder="RecipeName">
                </div>
                <div class="form-group col-md-6 mb-3">
                    <label for="inputemail">CreateDate</label>
                    <input type="text" class="form-control mt-1" name="CreateTime" placeholder="CreateTime">
                </div>
            </div>
            <div class="row">
                <div class="form-group col-md-6 mb-3">
                    <label for="inputsubject">料理時間Cooking time</label>
                    <input type="text" class="form-control mt-1" name="CookingTime" placeholder="CookingTime">
                </div>
                <div class="form-group col-md-6 mb-3">
                    <label for="inputsubject">準備時間時間PreparationTime</label>
                    <input type="text" class="form-control mt-1" name="PreparationTime" placeholder="PreparationTime">
                </div>

            </div>
            <div class="row">
                <div class="form-group col-md-6 mb-3">
                    <label for="inputsubject">份量Yield</label>
                    <input type="text" class="form-control mt-1" name="Yield" placeholder="Model.Yield">
                </div>
            </div>
            @*食材清單*@
        <div id="ass_area">
            @{
                var index = 0;
                        <div class="row" id="row_@index">
                            <input type="text" class="form-control mt-1" name="Description[@index].Key" value="@index" hidden>
                            <input type="text" class="form-control mt-1" name="ProductID[@index].Key" value="@index" hidden>
                            <div class="form-group col-md-6 mb-3">
                                <label for="inputsubject">FOOD_Description</label>
                                <div class="row">
                                    <div class="col-md-2" style="float:left">
                                        <button id="ass_add" type="button" class="bi bi-plus form-control mt-1"></button>
                                    </div>
                                    <div class="col-md-10">
                                        <input type="text" class="form-control mt-1" name="Description[@index].Value" placeholder="Description">
                                    </div>
                                </div>
                            </div>
                            <div class="form-group col-md-6 mb-3">
                                <label for="inputsubject">Product</label>
                                <div class="=row">
                                    <div class="col-md-8">
                                        <select class="form-control mt-1" style="float: left;" name="ProductID[@index].Value">
                                            <option value="0">食材選擇</option>
                                            @{ foreach (var item in Model.Products)
                                                {
                                                    <option value="@item.ProductId">@item.ProductName </option>
                                                }
                                            }
                                        </select>
                                    </div>
                                    <div class="col-md-2" style="float: left;margin-left:5px;">
                                        @*<button type="button" class="form-control mt-1 col-md-2" onclick="del_row(this)" data-id="' + row_num + '">Del</button>*@
                                        <button type="button" class="form-control mt-1" onclick="del_row(this)" data-id="@index">Del</button>
                                    </div>
                                </div>
                            </div>
                        </div>
                    index++;
                    }
                    
            </div>
            <div class="mb-3">
                <label for="inputmessage">01.備料Preparation</label>
                <textarea class="form-control mt-1" name="Preparation" placeholder="Preparation" rows="8"></textarea>
            </div>
            <div class="mb-3">
                <label for="inputmessage">02.烹煮步驟Step</label>
                <textarea class="form-control mt-1" name="Step" placeholder="Preparation" rows="8"></textarea>
            </div>
            <div class="row">
                <div class="col text-end mt-2">
                    <button type="submit" class="btn btn-success btn-lg px-3">Send</button>
                </div>
            </div>
        </form>
    </div>
</div>

@*End Contact*@
@* select option，給js使用新增項目 *@
<select id="porduct_select_area" hidden>
    <option value="0">食材選擇</option>
    @{
        foreach (var item in Model.Products)
        {
            <option value="@item.ProductId">@item.ProductName </option>
        }
    }
</select>
@*End Contact*@



@section Scripts {
    <script>
        var loadFile = function (event) {
            var output = document.getElementById('output');
            output.src = URL.createObjectURL(event.target.files[0]);
            output.onload = function () {
                URL.revokeObjectURL(output.src) // free memory
            }
        };


        // ASS_ADD
        $('#ass_add').on('click', function () {
            console.log('114');
            // 計算現在有多少row
            var row_num = $('#ass_area').children('.row').length;
            var html = "";
            var option_html = $("#porduct_select_area").html();

            html += '<div class="row" id="row_' + row_num + '">';
            html += '<input type="text" class="form-control mt-1" name="Description[' + row_num + '].Key" value="' + row_num + '" hidden>';
            html += '<input type="text" class="form-control mt-1" name="ProductID[' + row_num + '].Key" value="' + row_num + '" hidden>';
            html += '<div class="form-group col-md-6 mb-3" >';
            html += '<label for= "inputsubject" > FOOD_Description</label >';
            html += '<input type = "text" class="form-control mt-1" name = "Description[' + row_num + '].Value" placeholder = "Description" >';
            html += '</div>';
            html += '<div class="form-group col-md-6 mb-3" >';
            html += '<label for= "inputsubject" > Product</label >';
            html += '<div class="row">';
            html += '<div class="col-md-8"><select class="form-control mt-1 style="float: left;"" name = "ProductID[' + row_num + '].Value" >';
            html += option_html;
            html += '</select> ';
            html += '</div>';
            html += '<div class="col-md-2" style=" float: left;margin-left:5px;"><button type="button"class="form-control mt-1" onclick="del_row(this)" data-id="' + row_num + '">Del</button></div>';
            html += '</div>';
            html += '</div>';
            console.log(html);
            $('#ass_area').append(html);
        });

        function del_row(obj) {
            var row_num = $(obj).data('id');
            $("#row_" + row_num).remove();
        }
    </script>
}


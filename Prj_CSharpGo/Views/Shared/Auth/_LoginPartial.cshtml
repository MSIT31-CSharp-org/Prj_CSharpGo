@*@model Prj_CSharpGo.Models.User

    <div class="container-fluid">
        <div class="row">
            <ul class="navbar-nav">
                @if (Model.IsSuccess == true)
                {
                    <li class="nav-item">
                        <a id="manage" class="nav-link" asp-area="Identity" asp-page="/Auth/Index">
                            <i class="fa fa-fw fa-user text-dark mr-3"></i>
                        </a>
                    </li>
                    <li class="nav-item">
                        <form id="logoutForm" class="form-inline" asp-area="Identity" asp-page="/Auth/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                            <button id="logout" type="submit" class="nav-link btn btn-link text-dark">
                                <i class="fas fa-sign-out-alt"></i>登出
                            </button>
                        </form>
                    </li>
                }
                else
                {
                    <li class="nav-item">
                        <a class="nav-link text-dark d-inline" id="register" asp-area="Identity" asp-page="/Auth/Register"><i class="fas fa-user-plus"></i>註冊</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-dark d-inline" id="login" asp-area="Identity" asp-page="/Auth/Login"><i class="fas fa-sign-in-alt"></i>登入</a>
                    </li>
                }
            </ul>
        </div>
    </div>*@